# TypeScript Config Package

Shared TypeScript configuration for frontend workspaces.

## Exports

- `base.json`: strict shared defaults for frontend TypeScript projects.
- `react-app.json`: Vite React app configuration for `apps/web` and `apps/admin`.
- `react-package.json`: buildable React package configuration for `packages/ui`.

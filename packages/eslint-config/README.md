# ESLint Config Package

Shared frontend ESLint configuration.

## Exports

- `@presstronic-academy/eslint-config/frontend`: factory for flat ESLint config in strict TypeScript React workspaces.

Consuming workspaces should define an `eslint.config.js` that passes its local directory as `tsconfigRootDir`.

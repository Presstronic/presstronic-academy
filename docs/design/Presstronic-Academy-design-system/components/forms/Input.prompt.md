Square text field on an inset graphite well; focus = cyan ring + faint glow.

```jsx
<Input label="Callsign" placeholder="you@domain.dev" hint="We never share this." />
<Input label="Access key" type="password" error="Access key is required." />
```

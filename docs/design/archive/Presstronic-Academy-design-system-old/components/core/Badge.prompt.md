Square mono badge for status and category readouts (never a pill, never emoji).

```jsx
<Badge tone="cyan">In progress</Badge>
<Badge tone="volt" variant="solid">Checkpoint</Badge>
```

Tones: neutral, cyan, volt (story/caution), green (success), red (danger). Variants: soft (default), solid.

spring:
  application:
    name: presstronic-academy-api

academy:
  api:
    application-name: Presstronic Academy API

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
